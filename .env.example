# This is a template for the required environment variables.
# Each developer should copy this file to a new file named .env.local
# and fill in their own secret values.
# DO NOT commit the .env.local file.

# From your Supabase project's Database settings (Direct Connection string)
DATABASE_URL="YOUR_SUPABASE_DATABASE_URL_HERE"

# A secret for Auth.js to encrypt tokens (generate a long random string)
AUTH_SECRET="A_SUPER_SECRET_RANDOM_STRING_HERE"

# You will add OAuth provider keys later, for example:
# AUTH_GITHUB_ID=""
# AUTH_GITHUB_SECRET=""